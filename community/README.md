# Awesome Codex Pet submission

`crt-monitor--your-handle/` is a placeholder example, not a ready-to-submit identity.

Generate the real folder with:

```bash
python3 ../scripts/prepare-community-submission.py YOUR_GITHUB_HANDLE
```

The generated pet folder contains exactly the three files required by the community submission contract.
