# Awesome Codex Pet submission

`crt-monitor--your-handle/` is a historical placeholder example, not a
ready-to-submit identity. Generated output is ignored by Git.

Generate the real folder with:

```bash
python3 ../scripts/prepare-community-submission.py YOUR_GITHUB_HANDLE --pet crt-monitor
```

The generated pet folder contains exactly the three files required by the community submission contract.
